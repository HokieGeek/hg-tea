import { SteeptimePipe } from './steeptime.pipe';

describe('SteeptimePipe', () => {
    it('create an instance', () => {
        const pipe = new SteeptimePipe();
        expect(pipe).toBeTruthy();
    });
});
